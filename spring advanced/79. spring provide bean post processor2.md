* AspectJExpressionPointcut
- execution(* hello.proxy.app..*(..))
  - * : all returnType
  - hello.proxy.app.. : package hello.proxy.app and sub packages
  - *(..) : all method name and parameters
- && !execution(* hello.proxy.app..noLog(..))
  - && : and
  - ! : not