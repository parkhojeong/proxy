*CGLIB constraints - concrete class proxy *
- use extends(inheritance)
  - need default constructor of super class
  - final class, can't inherit -> CGLIB exception occur
  - final method, can't override -> proxy not working