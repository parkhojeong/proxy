* AOP implementation method
1. compile time 
   - weaving(use aop compiler)
   - aspectj
2. class loading time. 
   - class loader manipulation. java instrumentation
   - aspectj 
   - ex. monitoring tool
3. runtime(proxy)
   - target class code is not changed
   - spring aop

* AOP appliance point 
- constructor, method, static method, access field
  - aspectj can apply all point (compile time, class loading time) 
  - spring aop join point: only method